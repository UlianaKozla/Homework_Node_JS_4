const create_flat = require('./create_flat');
const find_flat_by_id = require('./find_flat_by_id');
const find_all_flats = require('./find_all_flats');

module.exports = {
    create_flat,
    find_flat_by_id,
    find_all_flats
}